extends layout

block content
  h1= title
  
  p Do you really want to delete this ricpe?
    
    form(action=deleteId+'/delete', method="post")
      input(type="submit" class="btn btn-primary", Value="Delete")
    br
    br

  a(href="/recipes/"+deleteId, class="btn btn-xl btn-warning") Cancel
        